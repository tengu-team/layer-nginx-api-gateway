name: nginx-api-gateway
summary: Installs a NGINX api gateway
maintainers:
  - Sander Borny <sander.borny@ugent.be>
description: |
  Installs a NGINX api gateway
tags:
  - misc
subordinate: false
provides:
  website:
    interface: http
requires:
  upstream:
    interface: upstream
series: ['xenial']